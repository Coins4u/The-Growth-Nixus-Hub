import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing and Value Stack",
  description:
    "Explore transparent Growth Hub pricing, executive value-stack breakdown, and secure licensing paths for each membership tier.",
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
